

**Question 1**  
Using the properties `background-color` and `padding`, give a `div` a light-blue background color and add 20 pixels of padding.

**Question 2**  
Using the properties `background-color` and `padding`, set a `section`’s background to tomato color with a hexadecimal value and add 20 pixels of padding.

**Question 3**  
Using the properties `background-color` and `padding`, set an `article`’s background to pink using the RGB format and add 20 pixels of padding.

**Question 4**  
Using the properties `background-color` and `padding`, make an `aside` have a semi-transparent green background (30% opacity) with RGBA and add 20 pixels of padding.

**Question 5**  
Using the properties `background-image`, `background-repeat`, and `height`, apply the image `flower.png` to a `div` so it does not repeat and set the height to 200 pixels.

**Question 6**  
Using the properties `background-image`, `background-repeat`, and `height`, make the image `pattern.png` repeat only horizontally on a `section` and set the height to 100 pixels.

**Question 7**  
Using the properties `background-image`, `background-repeat`, and `height`, make the image `stripe.png` repeat only vertically on an `article` and set the height to 200 pixels.

**Question 8**  
Using the properties `background-image`, `background-repeat`, and `height`, make the image `dot.png` space itself evenly on an `aside` and set the height to 200 pixels.

**Question 9**  
Using the properties `background-image`, `background-attachment`, and `height`, fix the image `landscape.jpg` on the `body` so it stays in place while scrolling and set the height to 500 pixels.

**Question 10**  
Using the properties `background-image`, `background-attachment`, `height`, and `overflow`, make the image `bg-image.jpg` scroll normally with the content inside a `div`, set the height to 300 pixels, and allow scrolling.

**Question 11**  
Using the properties `background-image`, `background-attachment`, `height`, and `overflow`, make the image `texture.png` move with the local content inside a `section`, set the height to 200 pixels, and allow scrolling.

**Question 12**  
Using the properties `background-image`, `background-color`, and `height`, add the image `header-bg.jpg` to a `header`, provide a gray fallback color `#cccccc`, and set the height to 150 pixels.

**Question 13**  
Using the properties `background-image` and `height`, create a linear gradient from red to yellow (left to right) on a `div` and set the height to 100 pixels.

**Question 14**  
Using the properties `background-image`, `background-position`, `background-repeat`, and `height`, layer the images `star.png` and `cloud.png` on a `section`, place one at top-left and the other at bottom-right, stop both from repeating, and set the height to 200 pixels.

**Question 15**  
Using the properties `background-image` and `height`, create a radial gradient that starts as a blue circle and fades to white on an `article` and set the height to 200 pixels.

**Question 16**  
Using the properties `background-image`, `background-position`, `background-repeat`, and `height`, center the image `icon.png` both horizontally and vertically on a `div`, stop it from repeating, and set the height to 200 pixels.

**Question 17**  
Using the properties `background-image`, `background-position`, `background-repeat`, and `height`, position the image `logo.png` 50% from the left and 75% from the top on a `section`, stop it from repeating, and set the height to 200 pixels.

**Question 18**  
Using the properties `background-image`, `background-position`, `background-repeat`, and `height`, place the image `pattern.png` 10 pixels from the left and 20 pixels from the top on an `article`, stop it from repeating, and set the height to 200 pixels.

**Question 19**  
Using the properties `background-image`, `background-size`, `background-position`, `background-repeat`, and `height`, scale the image `bg.jpg` to completely cover an `article` (even if it crops), center it, stop it from repeating, and set the height to 200 pixels.

**Question 20**  
Using the properties `background-image`, `background-size`, `background-position`, `background-repeat`, and `height`, scale the image `pattern.png` to fit completely inside an `aside` without cropping, center it, stop it from repeating, and set the height to 200 pixels.
